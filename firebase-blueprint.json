{
  "entities": {
    "Summary": {
      "title": "Summary",
      "description": "A generated note summary with key points, keywords, and questions",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this summary"
        },
        "originalText": {
          "type": "string",
          "description": "Original raw text input by the user"
        },
        "summary": {
          "type": "string",
          "description": "Concise executive summary generated by Gemini"
        },
        "keyPoints": {
          "type": "array",
          "description": "Important bullet points extracted from the notes"
        },
        "keywords": {
          "type": "array",
          "description": "Key domain concepts and tags"
        },
        "questions": {
          "type": "array",
          "description": "Discussion and study questions formulated by Gemini"
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp in milliseconds when summary was created"
        }
      },
      "required": ["userId", "originalText", "summary", "keyPoints", "keywords", "questions", "createdAt"]
    }
  },
  "firestore": {
    "/summaries/{summaryId}": {
      "schema": "Summary",
      "description": "Collection of note summaries owned by users"
    }
  }
}
